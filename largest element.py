def largestElement(arr: [], n: int) -> int:
    # maxVal = arr[0]
    # Write your code from here.
    # for i in range(1,len(arr)):
    #     if(maxVal<arr[i]):
    #         maxVal = arr[i]
    # return maxVal
    return max(arr)
